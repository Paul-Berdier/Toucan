<%= form_with(model: touc) do |form| %>
  <% if touc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(touc.errors.count, "error") %> prohibited this touc from being saved:</h2>

      <ul>
        <% touc.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :touc %>
    <%= form.text_area :touc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
